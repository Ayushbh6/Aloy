# Optional API credentials. Populate only in an ignored .env file when needed.
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=
# Codex uses its supported local sign-in flow; do not copy tokens into this file.
